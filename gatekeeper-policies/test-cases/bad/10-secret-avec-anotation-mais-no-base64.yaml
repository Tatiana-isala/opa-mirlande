cat <<EOF | kubectl apply -f - || echo "Test réussi - la création a échoué comme prévu"
apiVersion: v1
kind: Secret
metadata:
  name: test-secret
  annotations:
    owner: "dev-team"
    environment: "staging"
type: Opaque
data:
  password: plain-text-password  # Non encodé en base64
EOF
